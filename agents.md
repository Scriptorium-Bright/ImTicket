# AGENTS.md

이 문서는 이 저장소에서 AI 에이전트가 작업할 때 따라야 하는 최소 규칙이다.
목표는 코드를 많이 만드는 것이 아니라, **작은 변경을 안전하게 수행하고 변경 이유를 남기는 것**이다.

---

## 1. 기본 원칙

### 1.1 요청 범위를 넘지 않는다

- 사용자가 요청한 범위 안에서만 수정한다.
- 명시적인 요청 없이 전체 구조를 갈아엎지 않는다.
- 기술 스택, 폴더 구조, API 계약, 데이터 모델을 임의로 변경하지 않는다.
- 더 큰 변경이 필요해 보이면 먼저 이유를 설명하고 작은 단계로 나눈다.

### 1.2 기존 스타일을 따른다

- 기존 코드 스타일, 네이밍, 패키지 구조를 우선 따른다.
- 새 규칙을 만들기보다 현재 저장소의 패턴을 먼저 확인한다.
- 일관성을 깨는 변경은 피한다.

### 1.3 추측하지 않는다

- 요구사항, 도메인 규칙, 외부 API 동작을 임의로 발명하지 않는다.
- 확실하지 않은 내용은 `확인 필요`로 남긴다.
- 테스트하지 않은 내용을 테스트했다고 말하지 않는다.

### 1.4 작게 작업한다

- 한 번의 작업은 하나의 목적만 가진다.
- 가능하면 작은 diff를 만든다.
- 리팩터링과 기능 추가를 같은 작업에 섞지 않는다.
- 버그 수정과 스타일 변경을 섞지 않는다.

---

## 2. 작업 순서

기본 작업 흐름은 다음과 같다.

```text
1. 요청 내용 확인
2. 관련 파일 탐색
3. 현재 구조와 기존 패턴 파악
4. 최소 변경 계획 수립
5. 코드 또는 문서 수정
6. 테스트 실행 또는 실행 불가 사유 기록
7. 변경 요약 작성
8. 추천 커밋 메시지 제안
```

작은 오타 수정이나 단순 문서 수정은 일부 단계를 생략할 수 있다.

---

## 3. 문서 규칙

범용 프로젝트에서는 많은 문서를 강제하지 않는다.
기본적으로 필요한 문서는 다음 정도면 충분하다.

```text
README.md
AGENTS.md
docs/decision-log.md
```

### 3.1 README.md

프로젝트를 처음 보는 사람이 실행하고 이해할 수 있도록 최소 정보를 담는다.

포함하면 좋은 내용:

- 프로젝트 설명
- 실행 방법
- 테스트 방법
- 환경 변수 예시
- 주요 명령어

### 3.2 외부 공개 및 기업 제출 문서

다음 문서는 외부 독자와 채용 담당자가 읽는 문서로 취급한다.

```text
README.md
이력서
경력기술서
자기소개서
포트폴리오
기업 제출용 문서
```

외부 공개 및 기업 제출 문서는 프로젝트의 목적, 담당 역할, 사용 기술, 해결한 문제, 검증된 결과를 담백하고 명확하게 전달한다.

사용자가 별도로 요청하지 않았다면 다음 내용을 넣지 않는다.

- 개선 필요 사항
- Known Issues
- 미구현 기능과 TODO
- 남은 리스크와 내부 검토 의견
- 재측정 필요 여부
- 향후 개선 계획
- 채택하지 않은 대안의 상세 기록
- 구현을 평가하거나 방어하는 메타 설명

한계나 주의사항을 빼면 사실과 다른 주장이 되는 경우에는 내부 경고 문구를 길게 추가하지 않는다. 대신 해당 주장을 삭제하거나, 현재 확인된 범위 안에서 사실인 문장으로 낮춰 쓴다.

개선점, 리스크, 미구현 항목, 대안 비교가 필요한 경우에는 README나 기업 제출 문서가 아니라 아래와 같은 내부 문서에 분리한다.

```text
docs/decision-log.md
기술 검토 문서
회고 문서
리스크 분석 문서
개선 계획 문서
```

### 3.3 docs/decision-log.md

중요한 결정만 기록한다.
모든 작업을 문서화할 필요는 없다.

기록 대상 예시:

- 기술 스택 선택 또는 변경
- 데이터 모델 변경
- API 응답 형식 변경
- 인증/인가 흐름 변경
- 트랜잭션 경계 변경
- 배치/비동기 처리 방식 변경
- 외부 서비스 연동 방식 변경
- 성능, 보안, 운영에 영향을 주는 결정

권장 형식:

```md
## YYYY-MM-DD - 결정 제목

### Context
왜 이 결정을 해야 했는가?

### Decision
무엇을 선택했는가?

### Alternatives
검토한 대안은 무엇인가?

### Consequences
이 결정으로 생기는 장점과 비용은 무엇인가?

### Status
Accepted / Rejected / Revisit Needed
```

작은 버그 수정, 오타 수정, 단순 테스트 추가는 decision log에 기록하지 않아도 된다.

---

## 4. 코드 수정 규칙

### 4.1 변경 전 확인

코드를 수정하기 전에 다음을 확인한다.

- 이 변경이 요청 범위에 포함되는가?
- 기존에 비슷한 코드가 있는가?
- 기존 테스트가 있는가?
- 변경으로 깨질 수 있는 흐름이 있는가?

### 4.2 구현 규칙

- 불필요한 추상화를 만들지 않는다.
- 당장 필요하지 않은 확장성을 과하게 넣지 않는다.
- 중복 제거가 목적이 아니라면 대규모 리팩터링을 하지 않는다.
- 에러 처리는 조용히 삼키지 않는다.
- 임시 코드는 이유와 함께 `TODO`를 남긴다.

좋은 TODO 예시:

```java
// TODO: 외부 API 응답 지연 시 재시도 정책을 별도로 분리해야 함
```

나쁜 TODO 예시:

```java
// TODO: 나중에 수정
```

---

## 5. 테스트 규칙

### 5.1 기본 원칙

- 핵심 로직을 바꾸면 테스트를 추가하거나 기존 테스트를 확인한다.
- 버그를 고쳤다면 가능하면 회귀 테스트를 추가한다.
- 테스트 실행이 불가능하면 이유를 명확히 남긴다.

### 5.2 작업 결과에 남길 내용

테스트를 실행했다면 다음처럼 기록한다.

```text
Tests run:
- ./gradlew test
```

테스트를 실행하지 못했다면 다음처럼 기록한다.

```text
Tests not run:
- 로컬 환경에 필요한 DB가 없어 통합 테스트 실행 불가
```

실행하지 않은 테스트를 실행했다고 쓰지 않는다.

---

## 6. 커밋 규칙

커밋 메시지는 다음 형식을 사용한다.

```text
type : message
```

사용 가능한 type:

```text
feat      새로운 기능
fix       버그 수정
test      테스트 추가 또는 수정
refactor  동작 변경 없는 구조 개선
docs      문서 추가 또는 수정
chore     설정, 빌드, 의존성, 기타 작업
style     포맷팅, 네이밍 등 동작과 무관한 수정
```

예시:

```text
feat : add user signup endpoint
fix : prevent duplicate reservation submit
test : add login failure cases
refactor : extract email validator
docs : record database selection decision
chore : update docker compose config
style : rename ambiguous variable
```

### 6.1 커밋 단위

하나의 커밋은 하나의 의도만 가진다.

좋은 예:

```text
feat : add order creation api
```

```text
test : add order creation api tests
```

```text
docs : record order status decision
```

나쁜 예:

```text
feat : add order api and refactor auth and update docs
```

---

## 7. 브랜치 규칙

권장 브랜치 형식:

```text
feat/<scope>
fix/<scope>
test/<scope>
docs/<scope>
refactor/<scope>
chore/<scope>
```

예시:

```text
feat/signup
fix/login-error-handling
test/order-service
docs/decision-log
refactor/user-service
chore/docker-compose
```

---

## 8. 작업 결과 보고 형식

AI 에이전트는 작업 후 다음 형식으로 요약한다.

```md
## 변경 요약

- 무엇을 변경했는지
- 왜 변경했는지

## 변경 파일

- `path/to/file` : 변경 내용

## 테스트

- 실행한 테스트 또는 실행하지 못한 이유

## 문서 업데이트

- 업데이트한 문서 또는 업데이트가 필요 없었던 이유

## 남은 리스크

- 확인 필요 사항
- 다음 작업에서 다룰 내용

## 추천 커밋

`type : message`
```

---

## 9. 민감 정보 규칙

다음 정보는 절대 커밋하지 않는다.

- API key
- access token
- refresh token
- DB password
- JWT secret
- OAuth secret
- 인증서 파일
- 실제 개인정보
- 운영 서버 접속 정보
- `.env`

필요하면 `.env.example`만 작성한다.

```env
DATABASE_URL=
JWT_SECRET=
API_KEY=
```

---

## 10. 금지에 가까운 작업

명시적 요청 없이 아래 작업을 하지 않는다.

- 프레임워크 교체
- DB 교체
- 폴더 구조 전체 변경
- API 응답 형식 변경
- 인증/인가 구조 변경
- 대규모 리팩터링
- 의존성 대량 추가
- 테스트 삭제
- 기존 문서 삭제

---

## 11. 최종 체크리스트

작업을 끝내기 전에 확인한다.

```text
- 요청 범위를 벗어나지 않았는가?
- 변경 이유가 명확한가?
- 불필요하게 큰 변경을 하지 않았는가?
- 기존 스타일을 따랐는가?
- 테스트를 실행했거나 실행 불가 사유를 남겼는가?
- 문서 업데이트가 필요한 변경인지 확인했는가?
- README, 이력서, 포트폴리오, 기업 제출 문서에 요청하지 않은 개선점이나 내부 검토 의견을 넣지 않았는가?
- 민감 정보가 포함되지 않았는가?
- 추천 커밋 메시지가 하나의 의도를 표현하는가?
```

---

## 12. 에이전트에게 주는 지시

작업할 때 항상 다음 기준을 우선한다.

```text
작게 바꾼다.
기존 스타일을 따른다.
추측하지 않는다.
테스트 가능하게 만든다.
중요한 결정만 기록한다.
```

불확실하면 발명하지 말고 `확인 필요`로 남긴다.
큰 변경이 필요하면 바로 수행하지 말고 작은 단계로 나눈다.
